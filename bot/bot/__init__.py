"""aiogram 3.x Telegram bot for the VPN shop.

The bot NEVER talks to the database or to the 3x-ui panel directly. It
only speaks HTTP to `backend` using the contract in
`shared/shared/contracts/http.py`.
"""

__version__ = "0.1.0"
